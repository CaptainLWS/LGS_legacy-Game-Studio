# Audio Module

Responsibilities:
- Sound playback and spatial audio handling.
- Sound event bus for playing/stopping audio effects.

Public API sketch:
- init(engineContext, options)
- playSound(soundId, opts)
- stopSound(instanceId)
- registerAudioSource(entityId, props)

