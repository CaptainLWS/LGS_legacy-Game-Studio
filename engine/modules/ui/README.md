# UI Module

Responsibilities:
- HUD components, simple overlays and debugging widgets.
- Coordinate with rendering module for draw layers.

Public API sketch:
- init(engineContext, options)
- createHUD(container)
- registerWidget(widget)

